# ═══════════════════════════════════════════════════════════════════════════
# NexaFlow Railway Deployment - Environment Variables Template
# Copy these to Railway Dashboard → [Variables] tab
# ═══════════════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────────────────
# 1️⃣  DATABASE CONFIGURATION (MySQL)
# ──────────────────────────────────────────────────────────────────────────

# Railway auto-generates DATABASE_URL from MySQL plugin
# Format: mysql://username:password@host:port/database

DATABASE_URL=mysql://root:${DB_PASSWORD}@${DB_HOST}:3306/nexaflow

# Individual database parameters (if using separate configuration)
DB_HOST=your-mysql-service.railway.internal
DB_PORT=3306
DB_NAME=nexaflow
DB_USER=root
DB_PASSWORD=your_super_secure_password_here

# ──────────────────────────────────────────────────────────────────────────
# 2️⃣  BACKEND - JAVA / SPRING BOOT CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────

# Server Configuration
SERVER_PORT=8080
SERVER_SERVLET_CONTEXT_PATH=/
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,metrics,info

# Spring Configuration
SPRING_PROFILES_ACTIVE=production
SPRING_APPLICATION_NAME=nexaflow-backend

# Database Connection
SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
SPRING_DATASOURCE_USERNAME=${DB_USER}
SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.MySQL8Dialect
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=false
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=true
HIBERNATE_DEFAULT_BATCH_SIZE=20

# ──────────────────────────────────────────────────────────────────────────
# 3️⃣  SECURITY & JWT
# ──────────────────────────────────────────────────────────────────────────

# 🔐 IMPORTANT: Generate a secure 32+ character random string
# Use online tools: https://generate-random.org/string-generator
# Or: openssl rand -base64 32

JWT_SECRET=your_must_change_this_to_a_secure_32_plus_char_string_for_production

# Token expiration (in milliseconds)
JWT_EXPIRATION=604800000

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.railway.app,https://yourdomain.com,http://localhost:3000
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOWED_HEADERS=*
CORS_MAX_AGE=3600

# Application URL (for links in emails, etc.)
APP_URL=https://your-backend-domain.railway.app

# ──────────────────────────────────────────────────────────────────────────
# 4️⃣  EMAIL CONFIGURATION (Brevo or Gmail SMTP)
# ──────────────────────────────────────────────────────────────────────────

# Option A: Gmail SMTP (Recommended)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your_app_specific_password
MAIL_FROM=NexaFlow <your-email@gmail.com>
MAIL_PROTOCOL=smtp
MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
MAIL_PROPERTIES_MAIL_SMTP_AUTH=true

# Option B: Brevo SMTP (Free 300 emails/day)
# MAIL_HOST=smtp-relay.brevo.com
# MAIL_PORT=587
# MAIL_USERNAME=your-brevo-email@gmail.com
# MAIL_PASSWORD=your_brevo_smtp_key
# MAIL_FROM=NexaFlow <noreply@yourdomain.com>

# ──────────────────────────────────────────────────────────────────────────
# 5️⃣  PAYMENT GATEWAY CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────

# Razorpay (India - UPI, Netbanking, Cards)
# Get keys from: https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_live_YOUR_KEY_ID_HERE
RAZORPAY_KEY_SECRET=YOUR_SECRET_KEY_HERE

# Stripe (International - Cards, Apple Pay, Google Pay)
# Get keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_YOUR_SECRET_KEY_HERE
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_PUBLISHABLE_KEY_HERE

# ──────────────────────────────────────────────────────────────────────────
# 6️⃣  FRONTEND - NODE.JS CONFIGURATION
# ──────────────────────────────────────────────────────────────────────────

# Server
PORT=3000
NODE_ENV=production

# Backend Connectivity
BACKEND_URL=https://your-backend-domain.railway.app
API_BASE_URL=https://your-backend-domain.railway.app/api
API_TIMEOUT=30000

# Frontend URL (for CORS origin)
FRONTEND_URL=https://your-frontend-domain.railway.app

# ──────────────────────────────────────────────────────────────────────────
# 7️⃣  JAVA RUNTIME OPTIMIZATION
# ──────────────────────────────────────────────────────────────────────────

# Memory and Performance
JAVA_TOOL_OPTIONS=-Xmx512m -Xms256m -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# Node.js Optimization
NODE_OPTIONS=--max-old-space-size=512

# ──────────────────────────────────────────────────────────────────────────
# 8️⃣  LOGGING & MONITORING
# ──────────────────────────────────────────────────────────────────────────

# Logging Levels
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_NEXAFLOW=DEBUG
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_WEB=INFO
LOGGING_LEVEL_ORG_HIBERNATE=WARN

# Monitoring
MANAGEMENT_ENDPOINTS_WEB_ENABLED=true
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=always

# ──────────────────────────────────────────────────────────────────────────
# 9️⃣  CURRENCY & LOCALIZATION
# ──────────────────────────────────────────────────────────────────────────

# Default currency
DEFAULT_CURRENCY=INR

# Exchange rate update interval (in hours)
EXCHANGE_RATE_SYNC_INTERVAL=6

# Timezone
TZ=Asia/Kolkata

# ──────────────────────────────────────────────────────────────────────────
# 🔟 OPTIONAL: AI & ADVANCED FEATURES
# ──────────────────────────────────────────────────────────────────────────

# Claude AI (for chat assistant)
ANTHROPIC_API_KEY=sk_ant_YOUR_ANTHROPIC_KEY_HERE

# Google Analytics (optional)
GOOGLE_ANALYTICS_ID=UA_YOUR_TRACKING_ID

# ═══════════════════════════════════════════════════════════════════════════
# RAILWAY DEPLOYMENT GUIDE
# ═══════════════════════════════════════════════════════════════════════════

# HOW TO USE THIS FILE:

# 1. Replace all placeholder values:
#    - DB_PASSWORD: Your secure MySQL password
#    - JWT_SECRET: Generate secure 32+ char string
#    - Email credentials: Your Gmail or Brevo details
#    - Payment keys: Your Razorpay and Stripe keys
#    - URLs: Your actual Railway domain URLs

# 2. Go to Railway Dashboard:
#    https://railway.app/dashboard

# 3. Select your NexaFlow project → [Variables] tab

# 4. Add all variables (copy-paste from above)

# 5. Each variable is added individually in Railway UI

# EXAMPLE RAILWAY DOMAIN URLS:
# Backend: https://nexaflow-backend-prod.railway.app
# Frontend: https://nexaflow-frontend-prod.railway.app

# ═══════════════════════════════════════════════════════════════════════════
# SECURITY NOTES
# ═══════════════════════════════════════════════════════════════════════════

# 🔐 DO NOT commit this file with real credentials to GitHub
# ✅ Add to .gitignore before committing
# ✅ Use Railway's built-in variable management
# ✅ Rotate JWT_SECRET every 6 months
# ✅ Use different passwords for dev/staging/production
# ✅ Enable Railway's environment protection

# ═══════════════════════════════════════════════════════════════════════════
